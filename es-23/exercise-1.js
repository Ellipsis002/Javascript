const person = {
  firstName: 'Mario',
  lastName: 'Rossi',
  age: 25
}

// Print values of person using Object.keys

let profile = Object.keys (person);
let profileValues = Object.values (person);
console.log(profile);
console.log(profileValues);